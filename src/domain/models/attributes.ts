/** Placeholder — B1 narrows to the settled `'normal' | 'foil'` union. */
export type Finish = string; // B1 narrows this
/** Placeholder — B1 narrows to the settled condition grades (NM|LP|MP|HP|DMG). */
export type Condition = string; // B1 narrows this
